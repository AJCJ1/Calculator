var tallyNum = 0;

function add(numToAdd) {
    tallyNum += numToAdd;
}
function subtract(numToRemove) {
    tallyNum -= numToRemove;
}
function multiply(numToMultiply) {
    tallyNum *= numToMultiply;
}
function divide(numToDivide) {
    tallyNum /= numToDivide;
}